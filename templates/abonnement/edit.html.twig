{% extends 'base2.html.twig' %}

{% block title %}Edit Abonnement{% endblock %}

{% block body %}
    <div style="text-align: center;">
        <h3 class="m-0 font-weight-bold text-primary" >Edit Abonnement</h3>
        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
        {{ include('abonnement/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-secondary" href="{{ path('abonnement_index') }}">back to list</a>

        {{ include('abonnement/_delete_form.html.twig') }}
    </div>

{% endblock %}
