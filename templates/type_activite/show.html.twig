{% extends 'base2.html.twig' %}

{% block title %}TypeActivite{% endblock %}

{% block body %}
<div class="container">
    <div class="card shadow mb-4">

        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Type d'activité</h6>
        </div>
        <div class="card-body">
            <div class="table-responsive">

                <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_activite.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_activite.type }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ type_activite.color }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('type_activite_index') }}">Retour à la liste</a>

    <a class="btn btn-warning" href="{{ path('type_activite_edit', {'id': type_activite.id}) }}">Modifier</a>

    {{ include('type_activite/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
