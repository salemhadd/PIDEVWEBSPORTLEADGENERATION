{% extends 'base.html.twig' %}

{% block title %}Seance{% endblock %}

{% block body %}
    <h1>Seance</h1>

    <table class="table">
        <tbody>


                <th>Capacite</th>
                <td>{{ seance.capacite }}</td>
            </tr>
            <tr>
                <th>Idcoach</th>
                <td>{{ seance.idcoach }}</td>
            </tr>
            <tr>
                <th>Dates</th>
                <td>{{ seance.dates }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('affrosea_index') }}">back to list</a>

    <a href="{{ path('affrosea_edit', {'idseance': seance.idseance}) }}">edit</a>

    {{ include('affrosea/_delete_form.html.twig') }}
{% endblock %}
