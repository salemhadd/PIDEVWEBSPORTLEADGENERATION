<form method="post" action="{{ path('etatreclamation_delete', {'id': etatreclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etatreclamation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
