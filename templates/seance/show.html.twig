{% extends 'base2.html.twig' %}

{% block title %}Seance{% endblock %}

{% block body %}
    <h1>Seance</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Idseance</th>
            <td>{{ seance.idseance }}</td>
        </tr>
        <tr>
            <th>Capacite</th>
            <td>{{ seance.capacite }}</td>
        </tr>
        <tr>
            <th>Idcoach</th>
            <td>{{ seance.idcoach }}</td>
        </tr>
        <tr>
            <th>Dates</th>
            <td>{{ seance.dates }}</td>
        </tr>
        <tr>
            <th>Activiteid</th>
            <td>{{ seance.activiteid }}</td>
        </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('seance_index') }}">back to list</a>

    <a  class="btn btn-primary" href="{{ path('seance_edit', {'idseance': seance.idseance}) }}">edit</a>

    {{ include('seance/_delete_form.html.twig') }}
{% endblock %}