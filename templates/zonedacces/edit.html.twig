{% extends 'base2.html.twig' %}

{% block title %}Edit Zonedacces{% endblock %}

{% block body %}
    <h1>Edit Zonedacces</h1>

    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    {{ include('zonedacces/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('zonedacces_index') }}">back to list</a>

    {{ include('zonedacces/_delete_form.html.twig') }}
{% endblock %}
