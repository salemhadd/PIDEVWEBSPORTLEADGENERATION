{% extends 'base2.html.twig' %}

{% block title %}New Zonedacces{% endblock %}

{% block body %}
    <h1>Create new Zonedacces</h1>
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    {{ include('zonedacces/_form.html.twig') }}


    <a class="btn btn-secondary" href="{{ path('zonedacces_index') }}">back to list</a>
{% endblock %}
