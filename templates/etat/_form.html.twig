{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
   {#  {{ form_widget(form) }}#}
<table ALIGN="Center" >
    <tr><td> {{ form_row(form.typeEtat,{'label':' typeEtat '} ) }}</td>

    </tr>
    <tr><td> {{ form_row(form.description,{'label':' Description '} ) }}</td>

    </tr>

    <tr><td> <button class="btn btn-success">{{ button_label|default('Save') }}</button></tr></td>
{{ form_end(form) }}
