{% extends 'base.html.twig' %}

{% block title %}Abonnement{% endblock %}

{% block body %}
    <h1>Abonnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonnement.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ abonnement.prix }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ abonnement.type }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ abonnement.activite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abonnement_front_index') }}">back to list</a>

    <a href="{{ path('abonnement_front_edit', {'id': abonnement.id}) }}">edit</a>

    {{ include('abonnement_front/_delete_form.html.twig') }}
{% endblock %}
