<form method="post" action="{{ path('zonedacces_front_delete', {'idzone': zonedacce.idzone}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zonedacce.idzone) }}">
    <button class="btn">Delete</button>
</form>
