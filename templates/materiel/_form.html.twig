{#  {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
 {#    {{ form_widget(form) }}
<table ALIGN="Center" >
    <tr><td>{{ form_row(form.nom,{'label':'Nom du Materiel'} ) }}</td>

    </tr>
    <tr><td>{{ form_row(form.marque,{'label':'Marque du Materiel'} ) }}</td>

    </tr>
    <tr><td> {{ form_row(form.quantite,{'label':'Quantite du Materiel'} ) }}</td>

    </tr>
    <tr><td> {{ form_row(form.image,{'label':'Image du Materiel'} ) }}</td>

    </tr>

    <tr><td>{{ form_row(form.idCategorie,{'label':'Categorie du Materiel'} ) }}</td>

    </tr>
    <tr><td> {{ form_row(form.idEtat,{'label':'Etat du Materiel'} ) }}</td>

    </tr>
    <tr><td align="Center"> <button class="btn btn-success">{{ button_label|default('Save') }}</button><tr><td>
{{ form_end(form) }}#}
<div class="container">

    <div class="card o-hidden border-0 shadow-lg my-5">
        <div class="card-body p-0">
            <!-- Nested Row within Card Body -->
            <div class="row">

                <div class="col-lg-7">
                    <div class="p-5">
                        <div class="text-center">
                            <h1 class="h4 text-gray-900 mb-4">Ajouter un materiel</h1>
                        </div>
                        {{ form_start(form,{'attr':{'novalidate': 'novalidate'}}) }}



                        <form class="user">
                            <div class="form-group row">
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_label(form.nom) }}
                                    {{ form_widget(form.nom,{'attr':{'class': 'form-control form-control-user'}}) }}
                                    {{ form_errors(form.nom) }}
                                </div>
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_label(form.marque) }}
                                    {{ form_widget(form.marque,{'attr':{'class': 'form-control form-control-user'}}) }}
                                    {{ form_errors(form.marque) }}
                                </div>
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_label(form.quantite) }}
                                    {{ form_widget(form.quantite,{'attr':{'class': 'form-control form-control-user'}}) }}
                                    {{ form_errors(form.quantite) }}
                                </div>
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_label(form.image) }}
                                    {{ form_widget(form.image,{'attr':{'class': 'form-control form-control-user'}}) }}
                                    {{ form_errors(form.image) }}
                                </div>
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_label(form.idCategorie) }}
                                    {{ form_widget(form.idCategorie,{'attr':{'class': 'form-control form-control-user'}}) }}
                                    {{ form_errors(form.idCategorie) }}
                                </div>
                                <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_label(form.idEtat) }}
                                    {{ form_widget(form.idEtat,{'attr':{'class': 'form-control form-control-user'}}) }}
                                    {{ form_errors(form.idEtat) }}
                                </div>




                            </div>
                            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
                            {{ form_end(form) }}
                            <a class="btn btn-light" href="{{ path('materiel_index') }}">Retour à la liste</a>
                        </form>

                    </div>
                </div>
            </div>
        </div>
    </div>

</div>
