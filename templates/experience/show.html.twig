{% extends 'base2.html.twig' %}

{% block title %}Experience{% endblock %}

{% block body %}
<div class="container">
    <div class="card shadow mb-4">

        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Experience</h6>
        </div>
        <div class="card-body">
            <div class="table-responsive">

                <table class="table table-bordered">



        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ experience.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ experience.titre }}</td>
            </tr>
            <tr>
                <th>Contenus</th>
                <td>{{ experience.contenus }}</td>
            </tr>
        </tbody>
    </table>

{% block afficher %}
<div class="col-md-3">   <a href="{{ path('experience_index') }}" class="btn btn-success">Retourner vers la liste</a></div>
{% endblock %}

    {% block editer %}
        <div class="col-md-3"> <a href="{{ path('experience_edit', {'id': experience.id}) }}" class="btn btn-warning">Modifier Experience</a></div>
    {% endblock %}


  {{ include('experience/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>


{% endblock %}
